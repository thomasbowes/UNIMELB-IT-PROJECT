install server:
    npm install

install client:
    cd client
    npm install
    cd..

start the server:
    npm run server

start the client:
    npm run client

start both:
    npm run dev

How to generate API Docs:

Foremost, you must globally download the apidoc module:
   npm install apidoc -g

Then:
   npm run docs